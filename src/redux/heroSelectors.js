export const heroesList = (state) => state.heroes;
export const totalItems = (state) => state.total;
export const heroFullInfo = (state) => state.heroCard;
export const Load = (state) => state.isLoading;
